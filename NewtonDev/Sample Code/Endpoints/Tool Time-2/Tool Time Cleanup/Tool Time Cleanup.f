/***      Newton Developer Technical Support Sample Code****      by Ryan Robertson, Newton Developer Technical Support****      Copyright © 1997 by Apple Computer, Inc.  All rights reserved.****      You may incorporate this sample code into your applications without**      restriction.  This sample code has been provided "AS IS" and the**      responsibility for its operation is 100% yours.  You are not**      permitted to modify and redistribute the source as "DTS Sample Code."**      If you are going to re-distribute the source, we require that you**      make it clear in the source that the code was descended from**      Apple-provided sample code, but that you've made changes.*/constant kToolTimePrefsTagString		:= "Tool Time:DTS";InstallScript := func( installPart, removeFrame )begin	// Grab a reference to the tool time preferences	local systemSoupCursor := GetStores()[0]:GetSoup( ROM_SystemSoupName ):Query( {type:		'index,																											indexPath:	'tag, 																											beginKey:	kToolTimePrefsTagString, 																											endKey:		kToolTimePrefsTagString} );	if systemSoupCursor:Entry() then		begin			EntryRemoveFromSoup( systemSoupCursor:Entry() );			GetRoot():Notify( kNotifyAlert, "Tool Time Cleanup", "Tool time prefs have been removed." );		end;	else		GetRoot():Notify( kNotifyAlert, "Tool Time Cleanup", "Could not find tool time prefs to remove." );end;