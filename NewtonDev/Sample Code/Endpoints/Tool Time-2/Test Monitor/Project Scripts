/***      Newton Developer Technical Support Sample Code****      by Ryan Robertson, Newton Developer Technical Support****      Copyright © 1997 by Apple Computer, Inc.  All rights reserved.****      You may incorporate this sample code into your applications without**      restriction.  This sample code has been provided "AS IS" and the**      responsibility for its operation is 100% yours.  You are not**      permitted to modify and redistribute the source as "DTS Sample Code."**      If you are going to re-distribute the source, we require that you**      make it clear in the source that the code was descended from**      Apple-provided sample code, but that you've made changes.*/constant kMonitorSymbol := '|TestMonitor:DTS|;// Because the InstallScript of a form part is EnsureInternal'd, we must// store our monitor frame in the part frame instead of using it as a constant.SetPartFrameSlot( 'theMonitorFrame,		{			MonitorEvent:	func(	theToolSymbol, theEvent, theData )								begin									print(theToolSymbol);									print(theEvent);									print(theData);								end,		});InstallScript := func( installPart )begin	// Install the tool monitor	call kInstallToolTimeMonitorFunc with ( kMonitorSymbol, installPart.theMonitorFrame );end;RemoveScript := func( removePart )begin	// remove the tool monitor	call kRemoveToolTimeMonitorFunc with ( kMonitorSymbol );end;// This is necessary so that the icon is filed in the extensions folderSetPartFrameSlot( 'labels, '_extensions );